server:
    port: 8080

spring:
    application:
        name: feed
    rabbitmq:
        addresses: localhost:5672

rabbitmq:
    exchange: tweet.exchange
    routing-key: tweet.routing.key
    queue: tweet.queue

tweet-service:
    host: localhost